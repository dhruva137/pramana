export function SandboxBadge() {
  return (
    <div className="sandbox-badge" role="status">
      Simulated sandbox — merchants &amp; injections are fixtures · Razorpay test mode only
    </div>
  );
}
